# Frontend runtime API endpoint
# Default is empty because service calls already include /api.
# Set this only if your frontend is hosted on a different origin from the gateway.
# Example: VITE_API_BASE_URL=http://127.0.0.1:4000
